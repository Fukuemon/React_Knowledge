import React from 'react';
import Header from '../../components/UserProfile/Header';

const UserProfile = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default UserProfile;
